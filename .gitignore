# Maven
target/
*.jar
*.war
*.ear
*.class
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.flattened-pom.xml
dependency-check-report.*

# IDEs
.idea/
*.iml
*.iws
*.ipr
.vscode/
*.swp
*.swo
*~

# Eclipse (also generated by maven-checkstyle-plugin)
.project
.classpath
.settings/
**/.checkstyle

# OS
.DS_Store
Thumbs.db

# Local environment files (demo credentials, etc.)
.env
.env.local

# Local Claude Code settings (don't rely on the global git ignore)
.claude/settings.local.json

# Local superpowers workspace (plans/specs not part of the repo)
docs/superpowers/

# Generated conformance run output — regenerate with the conformance test run
**/conformance-report.md
**/conformance-report.json

# Local developer scripts — repeatable helpers for tests/coverage/etc.
# Kept out of the public tree; conventions live in CLAUDE.md / dev notes.
dscripts/
